# Python dependencies for simdes courseware
# Pin exact versions for full reproducibility inside Docker.
# For local venv installs, minor version bumps are generally fine.

# Core simulation
simpy==4.1.1

# Scientific computing
numpy==1.26.4
scipy==1.13.0
pandas==2.2.2

# Visualization
matplotlib==3.9.0
seaborn==0.13.2

# Reinforcement learning (optional — heavy; skip if RL module not needed)
gymnasium==0.29.1
stable-baselines3==2.3.2
# torch is a transitive dependency of stable-baselines3;
# pin CPU-only build to keep image size manageable
# torch==2.3.0+cpu   # uncomment for CPU-only Docker image

# Input modeling
fitter==1.7.1

# Development / course tooling
jupyterlab==4.2.0
ipykernel==7.2.0
ipywidgets==8.1.3
nbformat==5.10.4

# Testing
pytest==8.2.0
pytest-cov==5.0.0

# Linting and type checking (omit from production Docker image if desired)
ruff==0.4.4
mypy==1.10.0
black==24.4.2
